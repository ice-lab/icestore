import Store from './store';
export default class Fiy {
    private stroes;
    registerStore(namespace: string, config: object): Store;
    private getModel;
    useStroe(namespace: string): void;
}
